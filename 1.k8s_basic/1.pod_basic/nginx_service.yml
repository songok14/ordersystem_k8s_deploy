apiVersion: v1
kind: Service
metadata:
# 클러스터 내에서 service 명은 url 주소로 사용
# ex) http://my-service
  name: my-service
  namespace: crong
spec:
  ports:
  # port는 서비스의 포트, targetPort는 pod의 포트
  - port: 80
    targetPort: 80
  selector:
    app: my-nginx